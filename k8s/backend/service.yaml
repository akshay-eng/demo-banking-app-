apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: banking-demo
  labels:
    app: backend
    tier: api
spec:
  selector:
    app: backend
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  type: ClusterIP
